[package]
name = "Lab_1_filling_polygon"
version = "0.1.0"
edition = "2024"

[dependencies]
raylib = {
    version = "6.0.0",
    features = [
        "SUPPORT_IMAGE_EXPORT",
        "SUPPORT_FILEFORMAT_BMP"
    ]
}